<%# 最適化ナビバー %>
<% current_path = request.path %>
<nav class="bg-white border-b border-gray-200 sticky top-0 z-50" aria-label="メインナビゲーション">
  <div class="container mx-auto flex items-center justify-between px-4 py-2">
    <div class="flex items-center gap-4">
      <%# タイトルロゴ：リンク装飾を完全に制御 %>
      <a href="/" class="no-underline text-xl font-bold text-blue-700 hover:text-blue-800" aria-label="ホーム" <%= 'aria-current="page"' if current_path == '/' %>>松江温泉マップ</a>

      <% unless current_path.start_with?('/admin') %>
        <%# 公開サイト用ナビゲーション %>
        <a href="/" class="no-underline text-gray-700 hover:text-blue-600 font-medium <%= current_path == '/' ? 'border-b-2 border-blue-600 text-blue-600' : '' %>" aria-label="検索・地図" <%= 'aria-current="page"' if current_path == '/' %>>検索</a>
        <a href="/admin/onsens" class="no-underline text-gray-700 hover:text-blue-600 font-medium" aria-label="管理画面">管理画面</a>
      <% else %>
        <%# 管理サイト用ナビゲーション %>
        <a href="/admin/onsens" class="no-underline text-gray-700 hover:text-blue-600 font-medium <%= current_path.start_with?('/admin/onsens') ? 'border-b-2 border-blue-600 text-blue-600' : '' %>" aria-label="温泉管理" <%= 'aria-current="page"' if current_path.start_with?('/admin/onsens') %>>温泉管理</a>
        <a href="/" class="no-underline text-gray-700 hover:text-blue-600 font-medium" aria-label="公開サイト">公開サイト</a>
      <% end %>
    </div>
    <div class="md:hidden">
      <%# ハンバーガーメニューは今後Stimulusで実装可 %>
    </div>
  </div>
</nav>
